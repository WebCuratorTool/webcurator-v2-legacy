schema.name=db_wct
schema.url=jdbc:postgresql://localhost:5432/Dwct
schema.user=usr_wct
schema.password=password
schema.driver=org.postgresql.Driver
schema.dialect=org.hibernate.dialect.PostgreSQLDialect
schema.query=select 1+1
schema.maxIdle=2
schema.maxActive=4
log4j.dir=logs/
log4j.level=DEBUG
attach.dir=/tmp/core/attachments/

mail.host=yourmailserver.domain.com
mail.protocol=SMTP
mail.port=25
intray.reply.email=noreply@yourdomain.com
core.host=localhost
core.port=8080
das.host=localhost
das.port=8080
das.uploaded.dir=/wct/store/uploadedFiles/
agent.host=localhost
agent.port=8080
ldap.url=ldap://yourldapserver.domain.com:389/
ldap.usr.dn=cn={0},ou=OrgUnit,o=Organisation
heritrix.version=1.14.1
project.web.site=http://yourwebsite.domain.com/
project.contact.email=youremail@yourdomain.com
http.proxy.name=
http.proxy.port=

# Set this property ONLY to enable double escaping in the
# browse tool. This is typically used when ModJK unescapes
# escaped character sequeunces. The value of the property does
# not matter; to turn this off, comment the attribute out
# altogether.
#browse.double_escape=true