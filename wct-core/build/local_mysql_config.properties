schema.name=DB_WCT
schema.url=jdbc:mysql://localhost:3306/DB_WCT
schema.user=usr_wct
schema.password=password
schema.driver=com.mysql.jdbc.Driver
schema.dialect=org.hibernate.dialect.MySQLDialect
schema.query=select 1+1
schema.maxIdle=5
schema.maxActive=10
log4j.dir=logs/
log4j.level=DEBUG
attach.dir=/tmp/core/attachments

mail.host=localhost
mail.protocol=SMTP
mail.port=25
intray.reply.email=youremail@yourdomain.com
core.host=localhost
core.port=8080
das.host=localhost
das.port=8080
das.uploaded.dir=/wct/store/uploadedFiles/
agent.host=localhost
agent.port=8080
ldap.url.build=ldap://yourldapserver.domain.com:389/
ldap.usr.dn=cn={0},ou=OrgUnit,o=Organisation
heritrix.version=1.14.1
project.web.site=http://yourwebsite.domain.com/
project.contact.email=youremail@yourdomain.com
http.proxy.name=
http.proxy.port=

#Needed for the null timestamp workaround
mysql_hack=true

# Set this property ONLY to enable double escaping in the
# browse tool. This is typically used when ModJK unescapes
# escaped character sequeunces. This can either be omitted,
# or set to true/false
browse.double_escape=false